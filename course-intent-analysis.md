# Codyssey AI/SW 기초 과정 — 전체 과제 의도 분석 및 학습 가이드

> 11개 구현 완료 과제에 대한 과제 원문 분석, 의도 파악, 학습 방향, 학습 내용, 학습 순서 정리
> 작성일: 2026-08-12

---

## 📖 목차

1. [과정 전체 설계 의도](#1-과정-전체-설계-의도)
2. [과제별 의도 분석](#2-과제별-의도-분석)
3. [학습 순서 및 흐름도](#3-학습-순서-및-흐름도)
4. [과제 간 연결 관계](#4-과제-간-연결-관계)

---

## 1. 과정 전체 설계 의도

### 1.1 과정 구조

```
Linux와 OS (B1)     → 서버 운영 기반
    ↓
Python과 Git (B2)   → 프로그래밍 기반
    ↓
자료구조/알고리즘 (B3) → 효율적 사고
    ↓
웹 기초/프론트엔드 (B4) → 사용자 화면
    ↓
데이터베이스/백엔드 (B5) → 데이터 저장 + 서버
    ↓
클라우드/AI API (B6)  → 외부 서비스 연동
    ↓
Term Project (B7)    → 통합 프로젝트
```

### 1.2 설계 의도

이 과정은 **"프레임워크를 배우기 전에 원리를 이해한다"**는 철학을 가집니다:

- B4-1에서 React 없이 순수 JS로 DOM 조작을 먼저 배움 → B4-2에서 React가 "왜" 필요한지 체감
- B5-1에서 프레임워크 없이 SQL을 직접 다룸 → B5-2에서 ORM이 "무엇을" 해주는지 이해
- B3-1에서 `dict`를 금지하고 해시 테이블을 직접 구현 → 내장 자료구조의 원리 체득
- B6-2에서 AI API를 직접 연동 → AI 도구의 한계와 제어 방법 체득

**각 과제의 제약사항이 곧 학습 의도입니다.** "금지"는 "직접 해보라"는 뜻입니다.

---

## 2. 과제별 의도 분석

---

### B1-1: 컴퓨터가 알아서 자기 상태를 점검하게 만들기

#### 과제 의도
서버 운영의 기본을 직접 체험. "명령어를 외우는 것"이 아니라 "왜 이 명령어가 필요한지"를 이해.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| SSH 포트 변경 (20022) | 기본 포트 22는 전 세계 스캔 대상 → 보안의 첫 단계 |
| 방화벽 (UFW) | "필요한 포트만 열기" = 최소 권한 원칙 |
| 계정/그룹/ACL | 역할별 접근 제어 = "모두가 모든 것에 접근" 방지 |
| 환경변수 (AGENT_HOME) | 실행 환경 고정 = "어디서 실행하든 같은 결과" |
| monitor.sh | 관제 자동화 = "사람이 24시간 볼 수 없으니 스크립트가 대신" |
| cron | 주기 실행 = "매분 확인하여 로그 누적" |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| Bash만 사용 | Python 등으로 대체하면 "셸 스크립트 작성 능력"을 체득할 수 없음 |
| sudo 최소화 | 일반 계정으로 진행 = 최소 권한 원칙 체득 |

#### 학습 목표의 의도
- "SSH 보안이 왜 기본인지" → 공격 시나리오 이해
- "방화벽 정책을 왜 구성하는지" → 네트워크 보안 원리
- "로그 보존이 왜 필요한지" → 장애 추적의 기반

#### 평가 질문 의도
| 질문 방향 | 의도 |
|-----------|------|
| "SSH 포트 변경이 왜 보안인가?" | 보안의 기본 원리(공격 표면 축소) 이해 확인 |
| "역할 기반 권한 분리 이유?" | 최소 권한 원칙 이해 확인 |
| "로그 보존 정책이 왜 필요?" | 운영 관점의 사고 확인 |

#### 학습 방향
1. Linux 기본 명령어 (ls, chmod, ps, ss, crontab)
2. 보안 개념 (SSH, 방화벽, 권한)
3. 셸 스크립트 작성 (변수, 조건문, 함수)
4. 자동화 (cron, 로그 관리)

---

### B1-2: 컴퓨터가 갑자기 느려지거나 멈췄을 때 원인 찾아 고치기

#### 과제 의도
실제 서버 장애(OOM, CPU Spike, Deadlock)를 재현하고 분석. "감"이 아닌 "데이터"로 원인 추론.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| OOM 재현 + 분석 | 메모리 누수 → 커널 OOM Killer → Before/After 수치 |
| CPU Spike 재현 + 분석 | CPU 독점 → Watchdog 강제 종료 → 임계값 조정 |
| Deadlock 재현 + 분석 | 스레드 교착 → PID는 살아있으나 무응답 → 원인 추론 |
| GitHub Issue 리포트 | 현업의 협업 방식 (증거 기반 커뮤니케이션) |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| 모니터링 로그 기반 | "로그 없으면 원인 추론 불가" 체득 |
| Before/After 수치 | "바꿨더니 됐다"가 아닌 수치 기반 검증 |

#### 학습 목표의 의도
- "관제 데이터로 원인 추론" → 데이터 기반 사고
- "GitHub Issue 형태 리포트" → 현업 커뮤니케이션
- "OS 동작 원리 설명" → 표면적 현상 아닌 깊은 이해

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "OOM 근본 원인?" | 메모리 관리(RSS, 힙, GC) 이해 |
| "Deadlock 4조건?" | 교착상태 이론 이해 |
| "스택 트레이스?" | 스레드별 상태 추적 능력 |
| "동시 장애 우선순위?" | 운영 판단력 |

#### 학습 방향
1. 프로세스/스레드 개념
2. 메모리 관리 (RSS, VSZ, OOM Killer)
3. CPU 스케줄링 (Watchdog, nice)
4. 동시성 (Deadlock 4조건, 경쟁 상태)
5. 관제 데이터 분석 + 기술 리포트 작성

---

### B2-1: 나만의 용돈 기입장 프로그램 만들기

#### 과제 의도
Python 기본기 + Git 워크플로우를 실제 프로그램 제작으로 체득.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| CRUD (입력/조회/수정/삭제) | 데이터 관리의 기본 패턴 |
| 파일 저장 (JSONL/CSV) | DB 없이 데이터 영속성 구현 |
| 예외 처리 | "프로그램이 죽지 않게" = 사용자 경험 |
| 통계 (수입/지출 합계) | 데이터 가공/집계 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| 외부 라이브러리 금지 | 표준 라이브러리만으로 구현 = 기본기 |
| 스택트레이스 출력 금지 | 사용자 친화적 에러 메시지 = UX 관점 |

#### 학습 목표의 의도
- "Python 기본 문법" → 변수, 함수, 클래스, 파일 I/O
- "Git 워크플로우" → commit, branch, merge
- "예외 처리" → try/except + 사용자 메시지

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "데이터 저장 방식 선택?" | JSONL vs CSV 장단점 이해 |
| "예외 처리 방식?" | 사용자 경험 관점 |

#### 학습 방향
1. Python 기본 문법 (변수, 조건, 반복, 함수)
2. 파일 I/O (open, read, write, JSON/CSV)
3. 클래스/객체 지향
4. 예외 처리 (try/except, 사용자 메시지)
5. Git 기본 (add, commit, push, branch)

---

### B3-1: 정보를 엄청 빠르게 찾아주는 작은 저장소 만들기

#### 과제 의도
`dict`를 금지하고 해시 테이블을 직접 구현 → "내장 자료구조가 어떤 원리로 동작하는지" 체득.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 해시 함수 구현 | "문자열 → 숫자" 변환 원리 |
| 충돌 해결 | 해시 충돌 시 대처 (체이닝/개방주소법) |
| put/get 연산 | O(1) 탐색의 원리 |
| 캐싱 (LRU) | "자주 쓰는 것은 빠르게" 최적화 사고 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| `dict`, `set`, `collections` 금지 | "편한 도구를 빼앗아 원리를 이해하게 함" |
| 고정 길이 배열만 허용 | 배열 + 해시 함수 = 해시 테이블의 최소 구성요소 |

#### 학습 목표의 의도
- "해시 함수가 왜 필요한지" → O(1) 탐색의 원리
- "충돌이 왜 발생하는지" → 해시 공간의 한계
- "시간 복잡도" → O(1) vs O(n) 체감

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "해시 함수 설계?" | 분산성/결정성 이해 |
| "충돌 해결 방법?" | 체이닝 vs 개방주소법 장단점 |
| "O(1)이 보장되는 조건?" | 해시 테이블의 전제 조건 |

#### 학습 방향
1. 배열과 인덱스 접근
2. 해시 함수 원리 (모듈러 연산, 분산)
3. 충돌 해결 (체이닝, 개방주소법)
4. 시간 복잡도 (O(1), O(n), O(log n))
5. 캐싱 개념 (LRU)

---

### B3-2: 파일이 언제 어떻게 바뀌었는지 기록하는 작은 프로그램 만들기

#### 과제 의도
정렬 알고리즘을 직접 구현 → `sorted()`가 내부적으로 하는 일을 이해.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 파일 변경 감지 | "언제 바뀌었는지" = 버전 관리의 기초 |
| 정렬 직접 구현 | `sorted()` 금지 → 정렬 알고리즘 원리 체득 |
| 변경 이력 관리 | "무엇이 바뀌었는지" = diff의 기초 |
| 탐색 (이진 탐색) | 정렬된 데이터에서 빠른 검색 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| `sorted()` 금지 | 정렬 알고리즘을 직접 구현하여 원리 이해 |
| 그래프 라이브러리 금지 | 자료구조를 직접 구현 |

#### 학습 방향
1. 정렬 알고리즘 (버블, 선택, 삽입, 퀵, 머지)
2. 시간 복잡도 비교 (O(n²) vs O(n log n))
3. 이진 탐색
4. 파일 시스템 감지 (mtime, hash)
5. 버전 관리 개념 (diff, snapshot)

---

### B4-1: 나를 소개하는 웹페이지 처음부터 만들기

#### 과제 의도
React를 배우기 전에 순수 HTML/CSS/JS로 "이벤트 → 상태 → 렌더링" 흐름을 직접 구현.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 시맨틱 HTML | 의미 있는 구조 → 접근성/SEO |
| CSS 변수 + 다크모드 | "한 곳에서 바꾸면 전체가 바뀜" = 단일 진실 공급원 |
| addEventListener | HTML과 JS 분리 → 관심사 분리 |
| state 객체 | "화면 = 상태의 함수" → React의 기초 |
| fetch + 4상태 | 비동기 처리의 현실 (로딩/에러/빈/성공) |
| localStorage | 상태 유지 → 새로고침 후에도 유지 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| React/Vue/jQuery 금지 | "프레임워크 없이 웹의 원리 체득" |
| `var` 금지 | 모던 JS 문법 습관 |
| `onclick` 금지 | HTML/JS 분리 습관 |
| 인라인 `style` 금지 | HTML/CSS 분리 습관 |

#### 학습 목표의 의도
- "이벤트 → 상태 → 렌더링" → React가 자동화하는 일의 원리
- "비동기 4상태" → 모든 웹 앱의 기본 패턴
- "DOM 조작" → React가 추상화하는 것

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "시맨틱 태그 왜?" | 접근성/SEO 이해 |
| "Flex vs Grid?" | 레이아웃 도구 선택 기준 |
| "addEventListener vs onclick?" | 관심사 분리 이해 |
| "이벤트→상태→렌더링 설명?" | React 학습의 기반 확인 |
| "4상태 왜 구분?" | 실무 UX 패턴 이해 |

#### 학습 방향
1. HTML 구조 (시맨틱 태그, 폼, 접근성)
2. CSS (변수, Flexbox, Grid, 반응형, 다크모드)
3. JavaScript (DOM, 이벤트, state, fetch, localStorage)
4. 비동기 처리 (async/await, try/catch, 4상태)
5. 배포 (GitHub Pages)

---

### B4-2: 버튼 누르면 화면이 스르륵 바뀌는 요즘 웹사이트 만들기

#### 과제 의도
B4-1에서 수동으로 하던 "이벤트 → 상태 → 렌더링"을 React가 어떻게 자동화하는지 체득.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 컴포넌트 분리 | "화면을 조각으로 나누는" 사고 |
| props vs state | "데이터가 어디서 오는지" 구분 |
| useEffect | "화면 그려진 후 실행" 시점 제어 |
| 라우팅 (SPA) | "새로고침 없이 화면 전환" |
| 커스텀 훅 | "로직을 빼내어 재사용" |
| Supabase/Firebase | BaaS 연동 → 백엔드 없이 데이터 CRUD |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| React 기반 | B4-1과 비교하여 "프레임워크가 해주는 일" 체감 |
| 백엔드 고급 기능 필수 아님 | "React 구조와 데이터 흐름"이 우선 |

#### 학습 목표의 의도
- "컴포넌트가 왜 필요한지" → B4-1의 "파일 3개" vs B4-2의 "컴포넌트 N개"
- "상태를 어디에 둘지" → 상향 배치 원칙
- "라우팅 → 컴포넌트 → 상태 → 이벤트 → 렌더링" → 전체 흐름

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "컴포넌트 쪼갠 기준?" | 설계 사고 확인 |
| "props vs state?" | 데이터 흐름 이해 |
| "useEffect 실행 시점?" | React 생명주기 이해 |
| "4상태 어떻게?" | B4-1 패턴의 React 적용 |

#### 학습 방향
1. React 기본 (JSX, 컴포넌트, props, state)
2. 이벤트 처리 + 상태 관리
3. useEffect + 의존성 배열
4. 라우팅 (React Router, HashRouter)
5. 커스텀 훅 (재사용 로직 분리)
6. BaaS 연동 (Supabase/Firebase)
7. 배포 (Vercel)

---

### B5-1: 정보를 깔끔하게 정리하는 디지털 서랍장 만들기

#### 과제 의도
프레임워크 없이 SQL을 직접 다루어 → ORM이 "무엇을 해주는지" 이해.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 테이블 설계 (PK/FK) | "데이터를 어떻게 구조화할지" 사고 |
| 1:N 관계 | "테이블 간 연결" = 관계형 DB의 핵심 |
| JOIN | "연결된 데이터를 한 번에 조회" |
| GROUP BY | "그룹별 집계" = 실무 요구 해결 |
| 15개 쿼리 | "요구사항을 SQL로 해결"하는 패턴 체득 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| 백엔드 프레임워크 금지 | "SQL을 직접 써서 원리 이해" |
| 뷰/프로시저/트리거 금지 | "기본에 충실" (과도한 고급 기능 회피) |

#### 학습 목표의 의도
- "엑셀과 DB의 차이" → "관계"가 핵심
- "PK/FK" → 데이터 무결성
- "JOIN/GROUP BY" → 실무 쿼리 패턴
- "인덱스" → 성능 최적화 기초

#### 학습 방향
1. DB 개념 (테이블, 행, 열, 관계)
2. SQL 기본 (CREATE, INSERT, SELECT)
3. 관계 (PK, FK, 1:N, N:M)
4. JOIN (INNER, LEFT, RIGHT)
5. 집계 (GROUP BY, COUNT, SUM, AVG)
6. 서브쿼리, 인덱스

---

### B5-2: 글을 쓰고·보고·고치고·지울 수 있는 게시판형 웹 서비스 만들기

#### 과제 의도
B5-1에서 SQL을 배운 후, FastAPI로 "요청 → 서버 → DB → 화면" 전체 흐름을 구현.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 레이어 분리 (Router/Service/Repository) | "역할별로 코드를 나누는" 구조적 사고 |
| PRG 패턴 | "새로고침 중복 방지" = 웹의 기본 패턴 |
| SQLAlchemy ORM | B5-1의 SQL을 Python 코드로 → ORM이 해주는 일 |
| Jinja2 SSR | "서버에서 HTML 생성" = 화면 렌더링 방식 |
| CRUD 화면 흐름 | "등록 → 목록 → 상세 → 수정 → 삭제" 전체 사이클 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| 지정 패키지만 사용 | "필요한 것만 쓰는" 최소 의존성 습관 |
| 인증/관계 구현 안 함 | "이번 미션은 CRUD + 구조에 집중" (다음 미션에서 확장) |

#### 학습 목표의 의도
- "요청이 서버를 거쳐 DB까지 가는 흐름" → 전체 아키텍처 이해
- "GET vs POST" → HTTP 메서드의 역할
- "PRG" → 웹 패턴의 이해
- "레이어 분리" → 유지보수 가능한 구조

#### 학습 방향
1. FastAPI 기본 (라우터, 요청/응답)
2. SQLAlchemy ORM (모델, 세션, CRUD)
3. Jinja2 템플릿 (SSR, 템플릿 상속)
4. PRG 패턴 (Post-Redirect-Get)
5. 레이어 분리 (Router → Service → Repository)
6. Pydantic Schema (요청/응답 모델)

---

### B5-3: 로그인이 되고 회원끼리 연결되는 웹 서비스 만들기

#### 과제 의도
B5-2의 CRUD에 인증/인가 + 연관관계 + 상태 전이를 추가 → "실제 서비스" 수준으로 확장.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| 인증 (로그인/로그아웃) | "너 누구야?" = 신원 확인 |
| 인가 (접근 제어) | "너 이거 해도 돼?" = 권한 검사 |
| 비밀번호 해싱 (bcrypt) | "평문 저장 금지" = 보안 기본 |
| 연관관계 (1:N) | "모델 간 관계" = 도메인 모델링 |
| 상태 전이 (DRAFT→PUBLISHED) | "단순 CRUD 너머의 비즈니스 로직" |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| 복잡한 권한 체계 요구 안 함 | "로그인/비로그인 구분"만으로 충분 (점진적 확장) |
| 지정 인증 패키지만 사용 | "필요한 것만" 최소 의존성 |

#### 학습 목표의 의도
- "인증 ≠ 인가" → 보안의 두 축 구분
- "Depends" → FastAPI의 의존성 주입 패턴
- "연관관계" → 객체 관점의 데이터 모델링
- "상태 변경 로직" → 비즈니스 규칙의 위치

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "인증 vs 인가?" | 보안 개념 구분 |
| "세션 vs JWT?" | 인증 방식 장단점 |
| "순환참조 해결?" | 양방향 관계의 함정 이해 |
| "상태 변경 어디에?" | 비즈니스 로직의 책임 위치 |

#### 학습 방향
1. 인증/인가 개념
2. 세션 기반 인증 (itsdangerous, SessionMiddleware)
3. 비밀번호 해싱 (passlib, bcrypt)
4. Depends (의존성 주입)
5. SQLAlchemy 연관관계 (1:N, back_populates)
6. 상태 전이 (Enum, 비즈니스 로직)
7. 순환참조 방지 (Pydantic Schema)

---

### B6-2: 내가 고친 코드 설명을 AI가 대신 써주는 도우미 만들기

#### 과제 의도
AI API를 "호출"하는 것이 아니라, AI 결과물의 "품질을 제어"하는 방법을 체득.

#### 기능의 의도
| 기능 | 의도 |
|------|------|
| Git 변경 사항 수집 | "git diff를 프로그램 입력으로" = 자동화 |
| 프롬프트 설계 | "어떻게 부탁하느냐가 품질을 결정" |
| API 파라미터 제어 | temperature/max_tokens로 결과 제어 |
| 민감정보 마스킹 | "AI에게 비밀을 보내지 마" = 보안 |
| 출력 검증 | "AI가 항상 형식을 지키는 건 아님" = 검증 필요 |
| Mock 모드 | "API Key 없이도 테스트" = 개발 효율 |

#### 제약의 의도
| 제약 | 의도 |
|------|------|
| API Key 환경변수만 | "코드에 Key를 적지 마" = 보안 기본 |
| 1회 실행 1~2회 호출 | "비용을 의식하는" 운영 관점 |
| 민감정보 프롬프트 제외 | "AI에게 데이터 유출 방지" |
| git push 금지 | "초안 출력까지만" (범위 제한) |

#### 학습 목표의 의도
- "AI API 연동 흐름" → 요청/응답/예외 전체
- "프롬프트 설계" → AI 결과물 품질 제어
- "파라미터 이해" → temperature/max_tokens 영향
- "보안" → 민감정보 마스킹 + 환경변수

#### 평가 질문 의도
| 질문 | 의도 |
|------|------|
| "temperature 영향?" | AI 파라미터 이해 |
| "프롬프트 설계 원리?" → AI 제어 능력 |
| "민감정보 처리?" | 보안 의식 |
| "출력 검증 왜?" | AI를 신뢰하지만 검증하는 태도 |

#### 학습 방향
1. AI API 개념 (chat/completions, 모델)
2. HTTP 요청 (requests, headers, body)
3. 프롬프트 설계 (역할, 양식, 맥락, 제약)
4. API 파라미터 (temperature, max_tokens, top_p)
5. Git 연동 (subprocess, git status/diff)
6. 보안 (환경변수, 마스킹, .env)
7. CLI 설계 (argparse, 명령/옵션)
8. 검증 (출력 형식, 길이, 템플릿)

---

## 3. 학습 순서 및 흐름도

### 3.1 과정 전체 학습 순서

```
Phase 1: 서버 기반 (B1)
  B1-1: Linux 서버 구축 + 모니터링 자동화
  B1-2: 시스템 장애 분석 + 트러블슈팅
    ↓ "서버를 운영할 수 있다"

Phase 2: 프로그래밍 기반 (B2)
  B2-1: Python 프로그램 + Git 워크플로우
  B2-2: 팀 협업 (Git Flow)
    ↓ "코드를 작성하고 관리할 수 있다"

Phase 3: 효율적 사고 (B3)
  B3-1: 해시 테이블 직접 구현 (dict 금지)
  B3-2: 정렬 알고리즘 직접 구현 (sorted 금지)
    ↓ "내장 도구의 원리를 이해한다"

Phase 4: 사용자 화면 (B4)
  B4-1: 순수 HTML/CSS/JS (프레임워크 금지)
  B4-2: React SPA (B4-1과 비교)
    ↓ "웹 화면을 만들 수 있다"

Phase 5: 데이터 + 서버 (B5)
  B5-1: SQL 직접 (프레임워크 금지)
  B5-2: FastAPI + ORM (B5-1 기반)
  B5-3: 인증/인가 + 연관관계 (B5-2 확장)
    ↓ "백엔드 서비스를 만들 수 있다"

Phase 6: 외부 서비스 (B6)
  B6-2: AI API 연동 + 프롬프트 설계
    ↓ "외부 AI 서비스를 활용할 수 있다"

Phase 7: 통합 (B7)
  B7-1: Term Project (전체 통합)
  B7-2: 고도화
```

### 3.2 과제 간 의존 관계

```
B1-1 (Linux 기반) → B1-2 (장애 분석)
B2-1 (Python+Git) → B3-1 (자료구조) → B3-2 (알고리즘)
B4-1 (바닐라 JS)  → B4-2 (React)     ← "원리 체득 → 프레임워크"
B5-1 (SQL 직접)   → B5-2 (FastAPI)   → B5-3 (인증+관계)
B5-2 (웹 서버)    → B6-2 (AI API)    ← "서버에서 외부 API 호출"
B4-2 (프론트엔드) + B5-3 (백엔드) → B7 (통합 프로젝트)
```

### 3.3 핵심 학습 흐름: "금지 → 이해 → 자동화"

| 단계 | 과제 | 패턴 |
|------|------|------|
| 직접 구현 | B3-1 | `dict` 금지 → 해시 테이블 직접 구현 |
| 직접 구현 | B3-2 | `sorted()` 금지 → 정렬 알고리즘 직접 구현 |
| 직접 구현 | B4-1 | React 금지 → DOM 조작 직접 구현 |
| 직접 구현 | B5-1 | 프레임워크 금지 → SQL 직접 작성 |
| 프레임워크 도입 | B4-2 | React 사용 → B4-1과 비교하여 이해 |
| 프레임워크 도입 | B5-2 | FastAPI+ORM → B5-1과 비교하여 이해 |
| 확장 | B5-3 | B5-2에 인증/관계 추가 → 실제 서비스 수준 |

---

## 4. 과제 간 연결 관계

### 4.1 "원리 → 추상화" 연결

| 원리 (직접 구현) | 추상화 (프레임워크) | 연결 |
|-----------------|-------------------|------|
| B3-1: 해시 테이블 직접 | Python `dict` | "dict가 어떻게 O(1)을 달성하는지" |
| B3-2: 정렬 직접 | Python `sorted()` | "sorted가 어떤 알고리즘을 쓰는지" |
| B4-1: DOM 직접 조작 | B4-2: React | "React가 자동으로 해주는 일" |
| B5-1: SQL 직접 | B5-2: SQLAlchemy ORM | "ORM이 SQL을 어떻게 생성하는지" |
| B5-2: 수동 세션 | B5-3: Depends 인증 | "의존성 주입이 인증을 어떻게 단순화하는지" |

### 4.2 "기본 → 확장" 연결

| 기본 | 확장 | 추가된 것 |
|------|------|----------|
| B1-1: 모니터링 | B1-2: 장애 분석 | OOM/CPU/Deadlock 분석 |
| B5-2: CRUD | B5-3: 인증+관계 | 로그인, 권한, 1:N, 상태 전이 |
| B5-2: 단일 모델 | B5-3: 3개 모델 | 연관관계, 순환참조 |
| B6-2: AI API | B7: 챗봇 프로젝트 | 대화형 AI, 웹 통합 |

---

> *이 문서는 Codyssey AI/SW 기초 과정의 11개 구현 완료 과제에 대한 의도 분석 및 학습 가이드입니다.*
